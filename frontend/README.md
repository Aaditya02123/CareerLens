# CareerLens frontend

This is the initial CareerLens frontend built with React, Vite, JavaScript, Tailwind CSS, and ESLint.

Run it locally with:

```bash
npm install
npm run dev
```

The current page is a minimal foundation. API calls, routing, authentication, and dashboard features will be added in later focused tasks.
